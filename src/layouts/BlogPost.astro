---
import BaseHead from '../components/BaseHead.astro'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'
import ReadingProgress from '../components/ReadingProgress.astro'

export interface Props {
  content: {
    title: string
    description: string
    pubDate?: string
    updatedDate?: string
    heroImage?: string
  }
}

const {
  content: { title, description, pubDate, updatedDate, heroImage },
} = Astro.props

const xSearchUrl = `https://x.com/search?q=${encodeURI(String(Astro.url))}`
---

<html lang="en" class="scroll-smooth">
  <head>
    <BaseHead title={title} description={description} image={heroImage} />
  </head>

  <body class="theme-shell">
    <Header />
    <ReadingProgress />

    <main class="min-h-screen">
      <article class="max-w-3xl mx-auto px-4 sm:px-10 py-12 lg:py-20">
        <!-- Navigation bar -->
        <div class="flex items-center justify-between mb-8">
          <a
            href="/blog"
            class="theme-link-muted inline-flex items-center text-sm group"
          >
            <svg
              class="w-4 h-4 mr-2 group-hover:-translate-x-1 transition-transform"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M15 19l-7-7 7-7"></path>
            </svg>
            Back to blog
          </a>
          <button
            data-title={title}
            data-text={description}
            data-url={Astro.url}
            hidden
            class="share-btn p-2 text-app-text-faint hover:text-app-text transition-colors cursor-pointer"
            aria-label="Share this article"
          >
            <svg
              class="w-5 h-5"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <circle cx="18" cy="5" r="3" stroke-width="2"></circle><circle
                cx="6"
                cy="12"
                r="3"
                stroke-width="2"></circle><circle
                cx="18"
                cy="19"
                r="3"
                stroke-width="2"></circle><path
                stroke-width="2"
                d="M8.59 13.51l6.83 3.98M15.41 6.51l-6.82 3.98"></path>
            </svg>
          </button>
        </div>

        <!-- Article header -->
        <header class="mb-12">
          <time class="text-sm text-app-text-soft block mb-4">
            {pubDate}
          </time>
          <h1
            class="text-3xl sm:text-4xl lg:text-5xl font-bold text-app-text mb-4 leading-tight"
          >
            {title}
          </h1>
          {
            updatedDate && (
              <p class="text-sm text-app-text-soft">
                Last updated on <time>{updatedDate}</time>
              </p>
            )
          }
        </header>

        <!-- Hero image -->
        {
          heroImage && (
            <div class="mb-12">
              <img
                src={heroImage}
                alt={title}
                class="w-full rounded-2xl ring-1 ring-app-border"
              />
            </div>
          )
        }

        <!-- Article content -->
        <div
          data-reading-progress-content
          class="theme-prose
          prose-headings:font-bold
          prose-h2:text-2xl prose-h2:mt-12 prose-h2:mb-4
          prose-h3:text-xl prose-h3:mt-8 prose-h3:mb-3
          prose-p:leading-relaxed prose-p:mb-6
          prose-a:text-green-600 prose-a:font-medium prose-a:underline prose-a:decoration-green-600/40 prose-a:underline-offset-4 hover:prose-a:text-green-700 hover:prose-a:decoration-green-700
          prose-strong:font-semibold
          prose-code:px-1.5 prose-code:py-0.5 prose-code:text-sm prose-code:before:content-none prose-code:after:content-none
          prose-ul:my-6 prose-ol:my-6
          prose-li:leading-relaxed
          prose-blockquote:relative prose-blockquote:my-10
          prose-blockquote:border-0 prose-blockquote:border-l-[0.2rem] prose-blockquote:border-l-green-500
          prose-blockquote:rounded-none prose-blockquote:bg-transparent prose-blockquote:py-[0.35rem]
          prose-blockquote:pl-[clamp(1.25rem,3vw,2rem)] prose-blockquote:pr-0
          prose-blockquote:text-app-text prose-blockquote:not-italic prose-blockquote:shadow-none
          [&_blockquote>p]:relative [&_blockquote>p]:m-0 [&_blockquote>p]:max-w-[42ch]
          [&_blockquote>p]:font-serif [&_blockquote>p]:text-[clamp(1.25rem,1.12rem+0.55vw,1.55rem)]
          [&_blockquote>p]:font-medium [&_blockquote>p]:tracking-[-0.015em]
          [&_blockquote>p]:leading-[1.45] [&_blockquote>p]:text-pretty
          [&_blockquote>footer]:mt-5 [&_blockquote>footer]:max-w-[58ch]
          [&_blockquote>footer]:font-sans [&_blockquote>footer]:text-[0.8125rem]
          [&_blockquote>footer]:font-medium [&_blockquote>footer]:not-italic [&_blockquote>footer]:tracking-normal
          [&_blockquote>footer]:leading-[1.65] [&_blockquote>footer]:text-app-text-soft
          [&_blockquote>p:first-of-type]:before:content-none
          [&_blockquote>p:last-of-type]:after:content-none
          [&_blockquote_a]:font-semibold [&_blockquote_a]:text-green-600 [&_blockquote_a]:no-underline"
        >
          <slot />
        </div>

        <!-- Social sharing -->
        <div class="mt-16 pt-8 border-t border-app-border">
          <div class="flex flex-wrap items-center gap-4">
            <span class="text-sm font-medium text-app-text"
              >Share this article:</span
            >
            <div class="flex gap-3">
              <button
                data-title={title}
                data-text={description}
                data-url={Astro.url}
                hidden
                class="share-btn theme-button-soft inline-flex items-center gap-2 px-4 py-2 text-sm font-medium rounded-lg cursor-pointer"
              >
                <svg
                  class="w-4 h-4"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <circle cx="18" cy="5" r="3" stroke-width="2"></circle><circle
                    cx="6"
                    cy="12"
                    r="3"
                    stroke-width="2"></circle><circle
                    cx="18"
                    cy="19"
                    r="3"
                    stroke-width="2"></circle><path
                    stroke-width="2"
                    d="M8.59 13.51l6.83 3.98M15.41 6.51l-6.82 3.98"></path>
                </svg>
                Share
              </button>
              <a
                target="_blank"
                rel="noreferrer noopener"
                href={xSearchUrl}
                class="theme-button-soft inline-flex items-center px-4 py-2 text-sm font-medium rounded-lg"
              >
                Discuss on X
              </a>
            </div>
          </div>
        </div>
      </article>
    </main>

    <Footer />

    <script is:inline>
      if (navigator.share) {
        document.querySelectorAll('.share-btn').forEach(function (btn) {
          btn.hidden = false
          btn.addEventListener('click', function () {
            navigator.share({
              title: btn.dataset.title,
              text: btn.dataset.text,
              url: btn.dataset.url,
            })
          })
        })
      }
    </script>
  </body>
</html>
